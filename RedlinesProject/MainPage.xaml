<Page
    x:Class="RedlinesProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RedlinesProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Margin="60" Spacing="60">
            <Button x:Name="MyButton" Content="Button"/>
            <CheckBox x:Name="MyCheckBox" Content="CheckBox"/>
            <Slider x:Name="MySlider" Width="100" HorizontalAlignment="Left"/>
        </StackPanel>
        <Canvas x:Name="RedlinesCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <local:ControlStateViewer x:Name="StateViewer" Grid.Row="1" Margin="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>

    </Grid>
</Page>
