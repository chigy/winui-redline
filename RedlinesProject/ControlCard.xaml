<Page
    x:Class="RedlinesProject.ControlCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RedlinesProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Visible">
        <Grid x:Name="ControlCardHolder" CornerRadius="2" MinWidth="450" Margin="0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Grid.Column="0" RequestedTheme="Light">
                <local:ControlStateViewer x:Name="StateViewerLightTheme" Margin="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            </Grid>

            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Grid.Column="1" RequestedTheme="Dark">
                <local:ControlStateViewer x:Name="StateViewerDarkTheme" Margin="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
