<UserControl
    x:Class="RedlinesProject.ColorViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RedlinesProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Margin="100" IsHitTestVisible="False">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Canvas x:Name="RedlineCanvas"/>
        <Grid Grid.Row="1"
            Margin="0,150,0,0" Width="300" HorizontalAlignment="Left">
            <Border BorderThickness="1" BorderBrush="Red" CornerRadius="2"/>
            <TextBlock x:Name="BrushTextBlock" Foreground="Red" Margin="20"/>
        </Grid>
    </Grid>
</UserControl>
